# Core image engine — handles JPEG, PNG, WebP, TIFF, BMP, GIF, ICO, TGA, PPM, PDF, etc.
Pillow>=10.0.0

# iPhone / modern formats: HEIC, HEIF, AVIF (read + write)
pillow-heif>=0.13.0

# Drag-and-drop support in the window (optional but nice; app works without it)
tkinterdnd2>=0.3.0

# Optional: uncomment to add RAW camera support (CR2, NEF, ARW, DNG, ...).
# These are larger/heavier wheels, so they are off by default.
# rawpy>=0.18.0
# numpy>=1.24.0
